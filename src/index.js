// Importing frameworks and components
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

// craeting element
const el = document.getElementById("root");
// creating root
const root = ReactDOM.createRoot(el);
// rendering app
root.render(<App />);
